# Dev

1. Ejecutar `npm i`
2. Clonar el .env.template y crear el .env.development
3. Ejecutar `docker compose up -d`
4. Ejecutar `npx prisma migrate dev --name init`
5. Ejecutar `npx prisma generate`
6. Ejecutar `npm run dev`
